<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Project_DataBase.ViewModel"
             x:Class="Project_DataBase.MainPage"
             Title="List of Catagories">

    <ContentPage.BindingContext>
        <local:CategoryViewModel/>
    </ContentPage.BindingContext>

        
    <StackLayout>
        <RefreshView Command="{Binding GetCatsCommand}" IsRefreshing="{Binding IsBusy}" >
        <CollectionView  
            ItemsSource="{Binding CatagoriesList}" 
            SelectionMode="Single" 
            SelectedItem="{Binding SelectedCatagory}"
            SelectionChangedCommand="{Binding SelectedCatagoryChangedCommand}">
          
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="10,20,10,20">
                            <Frame>
                                <Label Text="{Binding Cat_Name}" HorizontalOptions="Center" FontSize="Large"/>
                            </Frame>
                        </Grid>

                    </DataTemplate>
                </CollectionView.ItemTemplate>
     
            </CollectionView>
    </RefreshView>
    </StackLayout>
</ContentPage>
